<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Employee Visitor History</title>
    <style>
        body { font-family: Arial, sans-serif; text-align: center; }
        .history-container { width: 80%; margin: auto; text-align: left; }
        .date-section { margin-bottom: 20px; }
        .visitor-card { 
            padding: 10px; 
            border: 1px solid #ccc; 
            margin: 10px 0; 
            border-radius: 5px; 
            display: flex; 
            align-items: center;
        }
        .visitor-info {
            flex-grow: 1;
        }
        .visitor-image {
            width: 80px;
            height: 80px;
            border-radius: 50%;
            object-fit: cover;
            margin-left: 15px;
            border: 2px solid #ddd;
        }
        .approved { background-color: #d4edda; } /* Green for approved */
        .rejected { background-color: #f8d7da; } /* Red for rejected */
    </style>
</head>
<body>
    <h2>Employee Visitor History</h2>

    <div class="history-container">
        <% for (let visitor of visitors) { %>
            <div class="visitor-card <%= visitor.status.toLowerCase() %>">
                <div class="visitor-info">
                    <strong><%= visitor.fullName %></strong> - <%= visitor.status %> <br>
                    Contact: <%= visitor.contactInfo %> | Purpose: <%= visitor.purpose %> <br>
                    Host: <%= visitor.hostName %> (<%= visitor.hostEmail %>) <br> <!-- ✅ Host Email Added -->
                    Check-in: <%= visitor.checkInTime %> | Check-out: <%= visitor.checkOutTime %> <br>
                    <small>Updated at: <%= visitor.updatedAt ? visitor.updatedAt.toLocaleString() : "Unknown Date" %></small>
                </div>
                <% if (visitor.photoUrl) { %> 
                    <img src="<%= visitor.photoUrl %>" alt="Visitor Image" class="visitor-image"> <!-- ✅ Image Added -->
                <% } %>
            </div>
        <% } %>
    </div>
</body>
</html>
